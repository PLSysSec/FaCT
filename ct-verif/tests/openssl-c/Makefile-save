########## EDIT THE SECTION BELOW ##########
export ENTRYPOINTS =ssl3_cbc_remove_padding_wrapper
WRAPPERFILE       ?=ssl3_cbc_remove_padding_wrapper.c
############################################

export WD =${PWD}
VERIFDIR          ?=$(WD)/../../verifying-constant-time
BINDIR            ?=$(VERIFDIR)/bin

verify:
	EXAMPLE=$(WRAPPERFILE) make -C $(BINDIR) verify

clean:
	rm *.bpl
