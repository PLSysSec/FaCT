public int32 foo() {
  public bool x = true;
  public int32 y = 22;
  public uint32 z = 23;
  secret bool t = x;
  public int16 zz = (int16)z;

  public bool t_pub = declassify(t);

  secret int32 y_ = -25;
  public int32 yy = -y;
  public int32 yy_ = ~y;
  public bool xx = !x;

  //secret int32 tt = y + y_ - yy * yy_;

  /* overflow checking */
  //public int8 o1 = 120;
  //public int8 o2 = 112;
  //public int8 o3 = o1 + o2;

  secret int32 ttt = y | y_;
  secret int32 xxy = x ? yy : yy_;
}

public bool baz() {
}

public int32 bar() {
  secret int32 x = foo();
  public bool y = baz(4);
  y = x;
  if (x + 4) {
    y = x;
  } else if (x + 3) {
    x = y;
  } else {
    x = x + x;
  }

  for (int32 x = 0 to 6) {
    x = y;
  }

  baz(x, ref y);
  return 7;
  return;
}
