public int32 t(public int32[] x) {
  if (len x > 0) {
    return x[0];
  }
  return -1;
}

void iferror() {
  secret mut int8 c = 0;
  secret mut int8 d = 0;
  secret mut bool flag = false;

  if((c == 0) || (!flag)) {
    //blank
  }
}
