
secret int get100() {
  secret int x = 666;
  secret int x2 = 100;
  x = 10;
  return x2;
}

int identity(secret int my_int) {
  return my_int;
}

int mutateArray(secret bytearr[5] arr) {
  arr[4] = 44;
  return arr[4];
}

int mutateArray2(secret bytearr[5] arr2, secret int val) {
  arr2[4] = val;
  int f = 4;
  return arr2[f];
}

int simpleIf(secret int cond) {
  if (10 > cond) {
    return 1;
  } else {
    return 2;
  }
}

int mediumComplexIf(secret int cond) {
  secret int complex_ret = 10;
  if (10 > cond) {
    complex_ret = 1;
  } else {
    complex_ret = 2;
  }
  return complex_ret;
}

int mixedIf(secret int cond) {
  secret int complex_ret = 10;
  if (10 == cond) {
    return 1;
  } else {
    complex_ret = 2;
  }
  return complex_ret;
}

int mixedIf2(secret int cond) {
  secret int complex_ret = 10;
  if (10 > cond) {
    complex_ret = 1;
  } else {
    return 2;
  }
  return complex_ret;
}

int nestedIf(secret int cond) {
  if (10 > cond) {
    if (5 > cond) {
      return 1;
    } else {
      return 2;
    }
  } else {
    if (15 > cond) {
      return 3;
    } else {
      return 4;
    }
  }
}

int simpleLoop() {
  for (i = 0 to 10) {
    secret int a = 666;
  }
  return 10000;
}

int loopAcc() {
  secret int acc = 0;
  for (i = 0 to 5) {
    acc = acc + 1;
  }
  return acc;
}

int loopAssignArray(secret bytearr[5] arr) {
  for (i = 0 to 5) {
    arr[i] = i;
  }
  return 10000;
}

int add(public int a, public int b) {
  return a + b;
}

int add10And20() {
  return add(10, 20);
}

int addAll(secret bytearr[5] bytearr) {
  secret int acc = 0;
  for (i = 0 to 5) {
    acc = acc + bytearr[i];
  }
  return acc;
}

int multiply(secret int a, secret int b) {
  return a * b;
}

bool equal(secret int a, secret int b) {
  return a == b;
}

bool nequal(secret int a, secret int b) {
  return a != b;
}

int lshift(secret int num, secret int shift) {
  return num << shift;
}

int rshift(secret int num, secret int shift) {
  return num >> shift;
}

bool gt(secret int a, secret int b) {
  return a > b;
}

bool gte(secret int a, secret int b) {
  return a >= b;
}

bool lt(secret int a, secret int b) {
  return a < b;
}

bool lte(secret int a, secret int b) {
  return a <= b;
}

int neg(private int a) {
  return -a;
}

int xor(private int a, private int b) {
  return a ^ b;
}

int prec(private int a) {
  if (a == 1) {
    return 2 + 3 * 4;
  } else {
    if (a == 2) {
      return (2 + 3) * 4;
    } else {
      if (a == 3) {
        return -2 + 3;
      } else {
        return -1;
      }
    }
  }
}

int opassign() {
  private int a = 1;
  a += 5; // 6
  a -= 2; // 4
  a *= 3; // 12
  a <<= 3; // 12 * 8
  a >>= 2; // 24 == 0b11000
  a &= 0b101011; // 0b1000
  a |= 0b0101; // 0b1101
  a ^= 0b0110; // 0b1011 == 11
  return a;
}
